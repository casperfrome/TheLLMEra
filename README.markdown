这是一个为你的游戏《The LLM Era (大模型纪元)》准备的 `README.md` 文档。你可以将其保存在项目根目录下，既作为项目的说明文档，也可以作为玩家的游戏指南。

文档采用了 **Markdown** 格式，风格保持了赛博朋克/黑客风，与游戏主题契合。

---

```markdown
# The LLM Era (大模型纪元)

> "算力即权力，参数即真理。欢迎来到大模型纪元。"

**The LLM Era** 是一款基于 Go + HTML5 开发的集换式卡牌对战游戏。玩家将扮演一位“模型训练师”，通过收集主流 AI 大模型卡牌，利用 **Scaling Law（缩放定律）** 合成升级，并在 **Context Overflow（上下文溢出）** 战场中与其他模型进行算力对决。

## 🚀 快速启动

1. 确保已安装 Go 环境。
2. 在项目根目录下运行终端：
   ```bash
   go run main.go
   ```
3. 打开浏览器访问：`http://localhost:8080`

---

## 🎮 游戏核心玩法

### 1. 经济系统：算力 (Gold)
*   **初始资源**：新存档拥有 1000 算力（Gold）。
*   **获取方式**：
    *   **战斗胜利**：获得 50 算力。
    *   **战斗失败**：获得 10 算力（安慰奖）。
*   **消耗**：购买卡包（每包 100 算力）。

### 2. 卡牌系统
游戏目前收录了 6 种主流大模型，每种模型拥有不同的基础属性倾向：

| 模型名称 | 特性倾向 | 描述 |
| :--- | :--- | :--- |
| **GPT** | 均衡/强力 | 六边形战士，各项数值较高且平衡。 |
| **Claude** | 高防御 | 安全对齐（Alignment）极高，擅长持久战。 |
| **Gemini** | 高血量 | 超长上下文窗口，能承受更多伤害。 |
| **Llama** | 高攻击/脆皮 | 开源先锋，推理激进但防御较弱。 |
| **Mistral** | 极端输出 | 极致的推理能力，防御极低。 |
| **Grok** | 随机/均衡 | 数值波动较大，充满不确定性。 |

#### 属性说明
*   **HP (Parameters/参数量)**：模型的生命值，归零即崩溃。
*   **ATK (Inference/推理力)**：造成伤害的能力。
*   **DEF (Alignment/对齐性)**：抵消受到伤害的能力。
*   **Total (总算力)**：三维属性之和，决定库存排序。

---

### 3. 合成系统：Scaling Law
这是游戏变强的核心机制。遵循大模型的缩放定律，堆叠数量可以带来质变。

*   **规则**：集齐 **5张** 同名、同等级的卡牌。
*   **效果**：自动合成为 **1张** 高一级（Level +1）的卡牌。
*   **数值成长**：
    > 新卡属性 = (5张素材卡属性之和) / 2
    *   这意味着每一级提升，单卡属性约为原来的 **2.5倍**。
*   **视觉变化**：
    *   Level 0: 灰色边框
    *   Level 1: 蓝色边框
    *   Level 2: 紫色边框
    *   Level 3: 红色边框
    *   Level 4+: 金色呼吸特效 ✨

> **提示**：勾选界面上的 `[ ] 自动执行 Scaling Law`，系统会在抽卡后自动计算并执行合成，享受挂机变强的快感。

---

### 4. 战斗系统：Context Overflow
基于“温度（Temperature）”机制的 1v1 自动对战。

*   **准备阶段**：
    *   在库存中点击一张卡牌，卡牌边缘变绿并显示 **[EQUIPPED]** 即为出战模型。
    *   点击“寻找对手”，系统会匹配一个与你等级相近（或略强）的敌对模型。

*   **战斗机制：热度 (Temperature)**
    *   战斗开始时，双方温度为 0。
    *   每回合攻击后，模型温度上升（模拟显存发热/上下文积累）。
    *   **温度越高，不可控性越强**：
        1.  **幻觉暴击 (Hallucination)**：温度越高，越容易触发暴击（1.5倍伤害）。
        2.  **OOM 崩溃 (Out Of Memory)**：温度过高时，模型有概率发生内存溢出，导致**攻击失败并扣除自身 10% 血量**。

*   **策略**：高等级卡牌虽然属性强，但如果运气不好连续触发 OOM 自毁，也可能被低级卡牌反杀。

---

## 🛠️ 操作指南

*   **购买卡包**：点击按钮消耗 100G，获得 5 张随机属性卡牌。
*   **一键合成**：手动触发合成逻辑（如果未开启自动）。
*   **保存进度**：点击右上角按钮，将当前金币、库存、自动开关状态保存到本地 `data/save.json`。
*   **排序逻辑**：库存默认按 **总属性值 > 等级 > 类型** 排序，最强的卡永远在最前面。

---

## 📂 项目结构

```text
The-LLM-Era/
├── assets/         # 图片资源 (gpt.png, claude.png 等)
├── data/           # 存档文件夹 (自动生成 save.json)
├── static/         # 前端资源
│   ├── index.html  # 游戏主界面
│   ├── style.css   # 赛博朋克风格样式
│   └── game.js     # 核心游戏逻辑
├── main.go         # Go 后端服务
└── go.mod          # Go 模块定义
```

---

*Enjoy the Era of Intelligence.*
```